# 작업 메모리

핵심 기능 추가, 핵심 동작·화면 수정 또는 기능 제거가 완료될 때마다 이 파일을 업데이트한다.
각 기록에는 한국시간(KST) 기준 일시, 변경 유형, 변경 내용, 대상 파일과 검증 결과를 남긴다.
단순 문구 교정이나 영향이 없는 정리는 기록하지 않고, 기존 기록은 삭제하거나 덮어쓰지 않는다.

## 2026-08-04 15:52:37 KST · 현재 기준 상태 등록

- 유형: 핵심 기능 및 화면 기준 기록
- 내용: 중온 아스팔트 온실가스 산정 MVP, 모니터링 대시보드, 7단계 입력·검토·결과 흐름이 구현되어 있다.
- 내용: 방법론 적용조건 차단, 베이스라인·월별 활동자료, 증빙 연결, 단위변환, BE·PE·LE·ER 계산과 산정 제외 처리가 구현되어 있다.
- 내용: 그리너스 로고와 파비콘, 데스크톱·휴대폰 반응형 화면이 적용되어 있다.
- 대상 파일: `index.html`, `styles.css`, `app.js`, `assets/greeners-logo.png`, `assets/greeners-favicon.svg`
- 검증: 기존 구현 당시 브라우저 흐름, 계산 결과, 320px·390px 화면 폭과 콘솔 오류를 확인했다.

## 2026-08-04 16:25:49 KST · 프로젝트 파일 구조 정리

- 유형: 핵심 구조 수정 및 모바일 레이아웃 보완
- 내용: 최상위에는 바로 실행하는 `index.html`만 유지하고 CSS, JavaScript, 브랜드 에셋과 문서를 역할별 폴더로 이동했다.
- 내용: 새 폴더 경로에 맞게 스타일시트, 스크립트, 로고와 파비콘 참조를 수정했으며 외부 라이브러리는 추가하지 않았다.
- 내용: 사후 검증에서 발견된 320px 결과 화면의 가로 넘침을 주요 레이아웃 최소 폭 해제로 보완했다.
- 대상 파일: `index.html`, `css/styles.css`, `js/app.js`, `assets/brand/*`, `docs/*`
- 검증: 로컬 참조와 JavaScript 문법, 대시보드·결과 계산 흐름, 320px·390px 화면 넘침 및 브라우저 콘솔 오류 없음을 확인했다.

## 2026-08-04 16:36:58 KST · 반응형 로고 심볼 전환

- 유형: 핵심 반응형 화면 수정
- 내용: 1100px 이하에서는 가로형 `GREENers` 로고를 숨기고 원본 로고의 좌측 심볼만 잘라 표시하도록 변경했다.
- 내용: 데스크톱에서는 기존 전체 로고를 그대로 유지하며, 반응형 심볼은 별도 영역 안에서 비율을 유지한다.
- 대상 파일: `index.html`, `css/styles.css`
- 검증: 1024px·390px·320px에서 전체 로고 숨김, 심볼 표시·영역 내 배치, 가로 넘침 및 브라우저 콘솔 오류 없음을 확인했다.
